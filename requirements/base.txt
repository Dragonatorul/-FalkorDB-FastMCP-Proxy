# Base production requirements for FalkorDB FastMCP Proxy
# These are the minimal dependencies needed to run the application

# FastMCP framework
fastmcp>=2.10.2

# Core web framework dependencies  
fastapi>=0.116.0
uvicorn[standard]>=0.35.0
starlette>=0.46.0
sse-starlette>=2.4.0

# HTTP client
httpx>=0.28.1

# Authentication and security
pyjwt[crypto]>=2.8.0
cryptography>=45.0.0